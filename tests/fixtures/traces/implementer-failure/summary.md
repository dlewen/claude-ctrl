# Implementation — Tests Failed

The validation harness script encountered 2 failures: guardian-success fixture was classified as "partial" instead of "success" (SHA mismatch), and planner-timeout was classified as "partial" instead of "timeout" (duration calculation error).
